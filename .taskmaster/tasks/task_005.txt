# Task ID: 5
# Title: Create CodeCard Component with Syntax Highlighting
# Status: pending
# Dependencies: None
# Priority: high
# Description: Build reusable CodeCard component for displaying code snippets in hero section
# Details:
Create `src/components/CodeCard.tsx`:

**Visual Design:**
```
┌────────────────────┐
│ ● ● ●   Title      │ ← Header (macOS style)
├────────────────────┤
│ import { React }   │
│ from 'react';      │ ← Code content
│                    │
│ export const...    │
└────────────────────┘
```

**Props:**
```typescript
type CodeCardProps = {
  title: string;
  code: string; // Raw code string
  language?: 'typescript' | 'javascript';
};
```

**Styling:**
- Background: `bg-[#282c34]` (dark code editor)
- Border radius: `rounded-lg`
- Shadow: `shadow-xl`
- Header: `bg-gray-700` with 3 dots (red, yellow, green)
- Code font: `font-mono text-sm`
- Line height: `leading-relaxed`

**Syntax Highlighting (manual with spans):**
- Keywords (import, from, return, const): `text-purple-400`
- Strings: `text-green-300`
- Variables: `text-blue-400`
- Functions: `text-yellow-300`
- Numbers/params: `text-orange-400`

**Helper function:**
```typescript
const highlightCode = (code: string): JSX.Element => {
  // Split and wrap keywords/strings in colored spans
};
```

**NO external libraries:** Manual highlighting with regex or string manipulation

# Test Strategy:
Test with sample React and Express code, verify syntax colors render correctly
