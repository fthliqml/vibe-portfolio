# Task ID: 3
# Title: Create Data Structure and TypeScript Types
# Status: pending
# Dependencies: 1
# Priority: high
# Description: Setup data files with TypeScript types for personal info, projects, and tech stack
# Details:
Create data structure in `src/data/`:

**1. src/types/index.ts:**
```typescript
export type Project = {
  id: string;
  title: string;
  category: string;
  description: string;
  tech: string[];
  image: string;
  demoUrl?: string;
  repoUrl: string;
};

export type TechStack = {
  name: string;
  icon: React.FC<{ className?: string }>;
};

export type PersonalInfo = {
  name: string;
  title: string;
  email: string;
  description: string;
  social: {
    linkedin: string;
    instagram: string;
    email: string;
  };
};
```

**2. src/data/personal.ts:**
- name: "Muhammad Fatihul Iqmal"
- title: "Full-Stack Developer"
- description: Bio from hero section
- social links

**3. src/data/projects.ts:**
- Array of 3 projects (use placeholder data)
- All use `/images/project.jpg` initially

**4. src/data/techStack.ts:**
- Array of tech: React, TypeScript, Node.js, Express, MongoDB, PostgreSQL, Tailwind, Git

# Test Strategy:
Import data files, check TypeScript compiles, verify no errors
